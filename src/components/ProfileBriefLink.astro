---
import StyledLink from "./StyledLink.astro";

interface Props {
  url: string;
  text: string;
}

const { url, text } = Astro.props;
---

<StyledLink url={url} class="font-bold">{text}</StyledLink>
